## Estado
- Punto estable: Estructura del proyecto reorganizada y optimizada.
- 59 de 59 pruebas unitarias pasando exitosamente en GUT (100% de éxito).
- Escudos, enemigos, proyectiles, interfaz y utilidades completamente ordenados y funcionales con rutas corregidas.
- Pendiente de verificar en editor: polvo de pisadas (tamaño/cantidad del humo), VFX manual superpuesto a la flecha (recarga), target a 0.2 de altura, flecha cayendo que traspasa todo y explota a y=0.2; borrar DebugPunta al confirmar; correr GUT (59 + nuevos tests).

## Completado
- Fix Parser Error REAL #2: `Lonko.tscn` NO cargaba ("Could not preload resource file") → el sub_resource `Gradient` del polvo estaba mal serializado: `PackedColorArray(Color(...), ...)` y `PackedFloat32Array(0, 0.55, 1)` con enteros — el parser de .tscn NO acepta constructores `Color(...)` dentro de `PackedColorArray` ni ints en `PackedFloat32Array` (exige lista PLANA de floats, como serializa Godot: `PackedColorArray(0.62, 0.58, 0.52, 0.9, ...)` y `PackedFloat32Array(0.0, 0.55, 1.0)`). Validado con binario Godot 4.7.1 headless (Steam): `Lonko.tscn`, `Flecha_Electrica.tscn`, `Flecha_Electrica_Ataque.tscn`, `Marca_Zona_Caida.gd`, `Explocion_Pilar.tscn` cargan OK; Lonko.gd compila con autoloads al correr la escena. El error `_create_mesh_array must return at least a vertex array` observado en headless es artefacto del renderer dummy (mismo patrón QuadMesh+GPUParticles3D de Explocion_Pilar no lo produce con GPU; no afecta al juego).
- Fix Parser Error REAL #1: `Flecha_Electrica_Ataque.gd:203` usaba `vfx.dano = dano` pero `dano` NUNCA se declaraba en la clase → "Identifier dano not declared". Añadido `@export_category("Ataque Eléctrico - Daño")` + `@export var dano: float = 25.0` (mismo default que el VFX `Flecha_Electrica_VFX.gd`). El error previo de `_particulas_pisada` era fantasma (editor con caché vieja; 2 instancias de Godot abiertas).
- Polvo de pisadas al correr: nuevo nodo `GPUParticles3D` `Particulas_Pisada` en `Lonko.tscn` (hijo de la raíz, `unique_name_in_owner=true`, `emitting=false`, 14 partículas, lifetime 0.55 s) usando la textura `res://LONKO/humo pisada.png` (uid `uid://co802i012y46f`, ya importada). Config: `ParticleProcessMaterial` dirección UP con spread 40°, velocidad 0.15-0.45, gravedad -0.7, `emission_shape=BOX` (0.18×0.02×0.18) bajo los pies (y=0.08); `draw_pass_1` = QuadMesh 0.32 con billboard + `StandardMaterial3D` (unshaded, alpha, albedo tierra 0.62/0.58/0.52) + `color_ramp` `GradientTexture1D` con fade out (0→0.9→alfa 0). En `Lonko.gd`: `_particulas_pisada` resuelto en `_configurar_flecha_mano` vía `find_child` y cada `_process` emite SOLO en `current_state == State.WALKING` y sin daño/invulnerabilidad (`_particulas_pisada_emitir`) → se apaga al disparar, al pilar, al recibir daño y al morir. Nombres de variables/funciones en ASCII puro (`_particulas_pisada`, `_particulas_pisada_emitir`).
- VFX manual sincronizado (`Lonko.gd` + `Lonko.tscn`): `FlechaElectricaVFX_Manual` (instancia de `Flecha_Electrica.tscn` que el usuario colgó del `BoneAttachment3D_Arrow`) NO respetaba dirección (transform horneado gigante + rotación del hueso → rayo diagonal, "parecía otra flecha cargando"). Ahora en `_mostrar_flecha_electrica_en_mano` se hace visible SOLO durante la RECARGA eléctrica y se sincroniza con la flecha en mano: `global_transform = arrow.global_transform × Basis(rot_y(-90°))` → el rayo (-Z del VFX) apunta a la punta (+X de ArrowModel), ambas flechas SUPERPUESTAS y crecen con el tween. `_configurar_flecha_mano` y `_liberar_flecha_electrica_en_mano` lo ocultan (el proyectil en vuelo lleva su propio VFX) → al disparar/caer no queda "otra flecha" mirando a la cámara.
- Marca subida a Y 0.2: `Marca_Zona_Caida.gd` `offset_y` 0.05 → 0.2 (los aros flotan 0.2 sobre el piso).
- Caída de la flecha: TRASPASA todo lo estático (eliminado el bloque que explotaba contra `StaticBody3D`/`AnimatableBody3D` en `_on_body_entered`; escudos ya se atravesaban) y explota al llegar a `ALTURA_IMPACTO_SUELO = 0.2` en Y (nueva const + check en `_actualizar_movimiento` Fase.CAIDA). Daño a aliados/jugador se mantiene al contactar. Test `test_flecha_electrica_sube_marca_y_cae` actualizado: espera la caída hasta y<=0.2 y verifica destrucción en impacto.
- Fix target de aros INVISIBLE (`Marca_Zona_Caida.gd`): el fix "paralelo al piso" alineaba el aro con la rotación HORNEADA del glb `PISO ALIADO.glb` → aro DE CANTO (invisible) y normal derivada del glb dejaba la marca a ras de suelo. Ahora giro FIJO `-90° X` (círculo de frente desde la cámara lateral) y posición `PISO ALIADO2`.y + 0.05 con `Vector3.UP`. Eliminada `_normal_piso_aliado()`.
- Ataque eléctrico 2× MÁS RÁPIDO: `velocidad_subida` 20→40, `altura_cielo` 60→45, `segundos_marca` 3→1.5, `velocidad_caida` 12→24 (proyectil, `Lonko.gd` y override `Lonko.tscn` = 1.5); default `radio_marca` 2.5→1.25; test actualizado a 1.5 s.
- Orientación flecha eléctrica (punta ARRIBA en recarga y vuelo): eliminada la copia del transform del modelo normal + corrección quaternion en espacio global para la mano. Proyectil a la MITAD: escala raíz .tscn 0.5 + tween a `ONE * 0.5` + refuerzo en el disparo. Panel CONTROL DE SPAWN en NIVEL_DEBUG: esquina superior derecha + pausado por defecto.
- Recarga eléctrica = 2.0 s (duración de `Cargando SP.mp3`); marca SIN RayCast (directo sobre PISO ALIADO2); volteo de punta por script (`_flip_modelo_hacia_forward`); cubo DEBUG `DebugPunta` (x=1.355, BORRAR al confirmar); VFX lima recolorizado y rotado -90° Y; tests GUT flecha + marca (radio 1.25) + test orientación/escala.
- Tareas antiguas: ataque eléctrico completo (ciclo SUBIDA→ESPERA_MARCA→CAIDA, `_apuntar_arriba`, `_propagar_config_electrica`, VFX materiales); sonido Cargando SP (-6 dB); fix flecha gigante en mano; fix `ring_segments`; fix materiales VFX pisados; corrección tests Sombra 59/59; partículas hojas NIVEL01; velocidad flechas por ángulo; ragdoll Gárgola; Player Interior 3D; addon UID materiales; VFX FireBall tipo B.

## Pendiente
- Asegurar soporte de traducción completo.

- Fix Sobreescritura de Materiales VFX FireBall (`GargolaProjectile.gd`): causa raíz = `EnemyProjectileBase._ready` cachea TODOS los `MeshInstance3D` hijos (recursivo, `owned=false`) incluyendo los internos del VFX, y el método base `_restaurar_visuales_desde_pool` aplica `material_override = projectile_material` (rojo toon + outline negro) sobre ellos. Solución: override de `_ready` en `GargolaProjectile` que tras `super._ready()` filtra (`_excluir_meshes_vfx_del_cache`) los meshes del VFX fuera de `_cached_mesh_instances`. Defensa en profundidad junto al override existente de `_restaurar_visuales_desde_pool`/`_cleanup_materials`. Test `test_gargola_projectile.gd` reescrito a comportamiento VFX.
- Cambio de VFX a `VFX_Fire_ball_type_B.tscn`: reemplazada la constante `VFX_FIREBALL_SCENE` (antes `VFX_Fire_ball_standar.tscn`). Ajustada la compensación de escala a `0.25 / scale.x` (escala total del VFX en mundo = 0.25) y rotación del VFX a `rotation.y = -PI/2` para alinear su eje de avance (sobre Z) con el +X local del proyectil, de modo que apunte a la izquierda al disparar. Test actualizado al root `VFX_FireBall_TypeB`. Requiere recompilar/clear `.godot`.

### Reciente
- Player Interior 3D: Creado `Entities/Player/Player_Interior.gd` + `.tscn`. Nuevo personaje sin disparo con movimiento 3D WASD en plano XZ. Rotación del modelo `ArqueraModel` hacia 4 cardinales (eje dominante: N → -Z/W, S → +Z/S, E → +X/D, O → -X/A) usando `atan2(-d.z, d.x)` + `lerp_angle` suave (10°/s). AnimationTree dinámico con solo Idle ↔ Walk (`CAMINAR_ADELANTE`) vía `AnimationNodeTransition` xfade 0.2s. Mismo modelo PROTA.glb + CapsuleShape3D (ø0.4, h1.98) que Player original. Sin arco animado, sin huesos overrides, script mínimo ~105 líneas. Reemplazada instancia de `Player.tscn` en `Interio.tscn` por `Player_Interior.tscn` (misma posición/escala 0.09).
- Walkable Collider `Esctrucutra2` en `Nivel_Interior/Interio.tscn`: Convertido nodo a `StaticBody3D` + 4 `CollisionShape3D`, cada uno con BoxShape3D independiente para edición libre en editor.
- Addon Godot `addons/arquera_godot_tools/plugin.gd`: refactor `_asignar_uid_material`. Causa raíz: `.tres` files de Godot 4.7 embeben UID en **header del propio `.tres`** (línea `uid="uid://..."`), NO usan archivo `.uid` separado (como sí hacen `.gd`). Fix anterior escribía `.uid` file erróneamente. Reescrito para: (1) `get_resource_uid` (cache de ResourceLoader), (2) fallback parseo header `.tres` con `get_slice("\n",0)` + `find('uid="')`, (3) vacío como último fallback. `fs.scan()` movido ANTES de `_asignar_uid_material` para que Godot registre el `.tres` recién guardado y le embeba UID en el header. Añadidos `print("[ARQUERA] ...")` diagnósticos en 4 puntos clave para debug.
- Fix Material Externo Addon Godot `addons/arquera_godot_tools/plugin.gd`: causa raíz = `.uid` file NUNCA se escribía a disco. Función ahora (1) lee UID existente de `ResourceLoader`, (2) lee `.uid` file si existe, (3) crea UID + escribe `.uid` file manualmente con `FileAccess` (formato `uid://<id>\n`).
- Addon Godot `addons/arquera_godot_tools/plugin.gd`: reemplazados 2 botones por 1 "Procesar" + 2 CheckBox (`Check_Next_Pass`, `Check_Unshaded`). `_crear_material_final` ahora aplica `SHADING_MODE_UNSHADED` y `next_pass` solo si checkbox activo.

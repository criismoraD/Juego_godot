## Estado
- PlataformaMaderos atravesable por flechas + vasija con N pociones: los tablones mantienen colision para personajes pero las flechas de jugadora/aliadas/enemigas los atraviesan (deteccion por clase + padre en `Arrow`, preview del jugador, `EnemyProjectileBase` y rama aliada del tridente; el hacha pesada sigue clavandose por diseno). La vasija suma export `cantidad_pociones` (1-5): suelta N pociones curativas separadas, cada una con su curacion (antes `cantidad_municion` no aplicaba a pociones y solo salia una). Tests en `test_plataforma_maderos_flechas.gd` (3) y `test_vasija_contenedor.gd` (2).
- FIX ult de despedida del canon no danaba en el rio: el punto de caida quedaba fijo 1.4 s mientras la canoa avanzaba (caia al agua atras) y la explosion no tenia dano en area. Fix en `FlechaElectricaAtaque`: durante la marca el punto sigue a la canoa con jugadora a bordo (y mueve la marca; sin canoa queda fijo y esquivable como en NIVEL01) + dano en area al impactar (radio 0.7 ~ marca: jugadora dano+paralisis, aliadas solo paralisis, sin duplicar el golpe directo). Exports `seguir_canoa_en_marca` y `radio_dano_area`. Test nuevo `Tests/Unit/test_ult_canon_sigue_canoa.gd` (4: seguimiento, fijo sin canoa/abordo, area sin duplicar, fuera de radio).
- Globo: tripulante apunta hacia abajo a la canoa + inclinacion leve al avanzar. `GoblinTripulante._actualizar_apuntado()` inclina el cuerpo hasta 30 grados hacia la canoa (suavizado 5.0, solo picado, desactivable) siguiendo al jugador cada frame activo. `GloboAerostatico` suma proa-abajo constante de 3 grados al avanzar (se nivela solo al detenerse/pausa/muerte, sin pelear con el bamboleo del pivot). Exports ajustables. Tests en test_goblin_tripulante (3: picado acotado, no apunta arriba, flag off) y test_globo_aerostatico (2: objetivo segun fase, convergencia y nivelado suave).
- FIX hacha de Perrena danaba a la jugadora en el rio: `_es_entidad_a_ignorar` no contemplaba bando propio (solo al tirador) y la jugadora (capa 1, en la mascara del hacha) recibia `take_damage` en la canoa. Fix: el hacha solo la lanza la defensora aliada -> ignora siempre a `player` y `allies` (las atraviesa). Tests en test_defensora_perrena (ignorar + impacto sin dano ni clavado).
- Perrena en canoa se levanta con Pararse acelerado tras misil/mina: `MisilSubmarino` y `MinaAcuatica` ganan export `velocidad_animacion_pararse = 1.35` y `_reproducir_pararse()` (pararse primero y acelerado + regreso a reposo de canoa temporizado por duracion real del clip, sin pisar combate/muerte/nueva caida). Tests con sosias con _play_anim espia en test_jefe_submarino (secuencia completa) y test_mina_acuatica (velocidad+clip).
- Sin linea negra en la torre (jugadora y Perrena NPC): `PlayerInterior._quitar_linea_negra` y `PerrenaNPCInterior._quitar_linea_negra` retiran el next_pass de contorno (duplicando solo si hay algo que quitar, sin mutar compartidos) y sacan las mallas del grupo outline_meshes para que el toggle global de bordes no lo reponga. Tests: `test_player_interior_sin_linea_negra_en_torre` nuevo y `test_npc_material_de_perrena_sin_linea_negra` actualizado.
- FIX estandarte blanco del imp embajador en el barco pirata: `_animar_aparicion_tripulante` leia el material del recurso GLB (`mesh.surface_get_material`) saltandose el override de superficie de la instancia, donde vive la textura real (TELA con shader de viento) -> dissolve sin textura = blanco permanente. Fix: mismo orden que el dissolve de muerte (override, override de superficie, recurso) + soporte ShaderMaterial con albedo_texture; sin textura base se omite (sombras). Test en `test_balsa_pirata_combate.gd` (palo y tela disuelven con textura, sombra omitida).
- OPT primeros niveles (bajones FPS/lag): (1) Torre: la rama `IluminacionAnterior` (11 luces con sombra) renderizaba siempre junto a la Actual; ahora nace apagada en tscn y `Interio._ready` fija el modo actual. (2) Flecha: chequeo fuera de pantalla cada 3 frames fisicos + camara cacheada. (3) Trayectoria explosiva: prediccion con cientos de raycasts a 30 Hz + reticle/centro cacheados (antes find_child cada frame). Test nuevo `Tests/Unit/test_rendimiento_presupuestos.gd` (4: anterior apagada + max 3 sombras visibles en torre, tope 2 sombras en NIVEL01/tutorial, cadencia offscreen al 3er frame, trayectoria a 30 Hz). No se toco: paneles debug, mezcla de audio, outlines ni diseno de oleadas (medir conProfiler antes de recortar).
- Tramo acelerado con viento hasta el fin del Río + música garantizada bajo la cortinilla: el tramo rápido ahora llega hasta x_fin_nivel (antes terminaba en el barco ~126): sin enemigos cerca la canoa va a 1.8 con estelas de viento automáticas (el control manual de debug Z no se pisa gracias al flag `_viento_manual_debug`); con enemigos se apaga como antes. Al terminar el nivel, si no suena nada (gap tras la fanfarria del jefe) se arranca "Viaje por el rio" para que la música siga bajo la cortinilla negra (si ya suena algo no se interrumpe). Tests: 3 en test_canoa_reaccion_enemigos (viento auto, apagado con enemigos, manual), tramo hasta x=183 y música 7 en rio/transicion.
- FIX Parser Error en AzulinaPosando.gd (12 errores en cascada en el depurador): `const MATERIAL_AZULINA: Material` + `is StandardMaterial3D` no compila ("Expression is of type Material..."). Fix: const tipada `StandardMaterial3D` (el .tres lo es) y funcion simplificada (duplica, anula next_pass en la COPIA, el compartido conserva su outline). Verificado que no hay mas combos const-Material + `is` en el proyecto. Test nuevo `Tests/Unit/test_azulina_posando_material.gd` (2: copia sin outline, compartido conserva next_pass).
- Crucero de fondo fase 2 con bamboleo sutil y modelo reducido: `JefeSubmarinoRio` aplica `escala_modelo_fondo = 0.7` al entrar al fondo (perspectiva correcta a ~70 m) y bamboleo de navegacion sobre el pivot (sine Y 0.22 m + rolido 2.5 grados + cabeceo 1.5 grados, periodo 3 s), solo durante el crucero (no pelea con el tween de X ni con la base, que no corre en fase 2). Escala y pivot se restauran al completar, morir, debug X y volver a fase 1. Exports ajustables en categoria "Jefe - Fondo Fase 2". Tests en `test_jefe_submarino.gd` (3: aplicar/restaurar escala exacta, bamboleo mueve y se congela al completar, sin bamboleo fuera de fase 2).
- FIX lanzas de Azulina clavadas bloqueaban proyectiles: el Area clavada seguia siendo alcanzada por los rayos CCD (la flecha se teletransportaba al punto de impacto y, sin rama que la ignorara ni destruyera, se quedaba frenada). Fix en 2 niveles: (1) `EnemyProjectileBase` desactiva sus CollisionShape3D al clavarse (`_marcar_como_pegado`), destruirse (`_safe_destroy`) o volver al pool (`_desactivar_para_pool`) y los reactiva en `_activar_desde_pool` (cubre lanza, tridente, espada pirata via super y flechas enemigas); (2) el CCD de `Arrow.gd` ignora proyectiles (vivos o clavados, mismo criterio que `_es_objeto_ignorable_por_flecha` y el hacha). Test nuevo `Tests/Unit/test_lanza_azulina_colision_clavada.gd` (2: desactivar/reactivar formas, flecha atraviesa lanza clavada).
- Fin Río: la música sigue sonando bajo la cortinilla + título "Asalto al rio - Superado" + auditoría de traducciones. `AudioManager.stop_all(incluir_musica=true)` (default intacto); `PantallaFinNivel` suma export `mantener_musica_al_cubrir=true` y el barrido salta el player de música. CSV: `RIO_ASALTO_SUPERADO` ahora "Asalto al rio - Superado" (mayusculado en pantalla). Auditoría de textos de jugador → `tr()`: HUD oleada/restantes, pausa (título, continuar, reiniciar, menú, salir, Perrena, audio, volúmenes, calidad/resolución/fullscreen + 3 niveles), botones HUD play/pausa, badge galería, skip de splash e intro, ResultadoPacifista (título + 2 botones, nueva clave `BTN_REINICIAR_PARTIDA`); 21 filas es+en + mocks `stop_all` actualizados + test de claves en test_game_ui. EXCLUIDO a propósito: paneles debug/dev, valores numéricos, placeholders sobrescritos en runtime y `UIVidaJefe` sin uso.
- FIX flechas clavadas flotando en el aire junto al pilar de Lonko:
  La columna cilíndrica del modelo 3D mide en realidad 0.213 m en local (0.64 m en mundo con escala 3.0), mientras que el hitbox anterior tenía X = 0.28 (0.84 m en mundo), dejando un saliente invisible de 10 cm hacia la izquierda donde la punta de la flecha se clavaba en el aire. Se ajustó `size.x` de `BoxShape3D_pilar` en `PilarLonko.tscn` a 0.18 (0.54 m en mundo), asegurando que la flecha penetre ~5 cm en la superficie visual del pilar quedando firmemente incrustada sin flotar. Se mantuvo `size.z = 0.7` (2.1 m en mundo) garantizando cobertura del plano Z de la jugadora en la canoa (-7.5). Test actualizado y pasando 5/5 en `Tests/Unit/test_pilar_lonko_dano_flecha.gd`.
- Contorno toon 2D negro en escudos enemigos y redistribución de arqueras Lonko en Oleada 5:
  1. Escudos enemigos (`Escudo_enemigo.tscn`, `EscudoRoto_enemigo.tscn`): añadido `next_pass` con `TOON_LINEANEGRA.gdshader` (ancho 20.0, color negro) en `Entities/Ambiente_Escudo/Escudonivel3_MAT.tres`. En `Escudo.gd` y `EscudoRoto.gd` todas las mallas se registran en el grupo `outline_meshes` en `_ready()` y `_crear_material_flash()` preserva `next_pass` del material original para que el parpadeo de daño rojo no elimine el contorno negro. En `GuardianaMoradita.gd` se registran mallas en `outline_meshes`.
  2. Oleada 5: redistribución equilibrada de las 12 arqueras Lonko en `WaveSpawner.gd`. Los últimos 4 enemigos de la cola de 40 se reservan exclusivamente para unidades regulares (la oleada nunca termina con Lonkos). Las 12 Lonkos se intercalan homogéneamente entre los enemigos regulares (al menos 2 enemigos regulares entre cada Lonko, primera Lonko en los primeros 3 spawns).
  3. Límite de concurrencia activo de Lonkos (`max_lonkos_activos = 2`): si ya hay 2 Lonkos vivas en combate, `WaveSpawner._spawn_goblin()` busca el siguiente enemigo no-Lonko en la cola para spawnearlo primero, posponiendo a Lonko hasta que el jugador elimine a una de las presentes, evitando que 3 o más Lonkos abrumen con pilares y flechas eléctricas.
  4. Tests unitarios GUT en `Tests/Unit/test_oleada_5_distribucion_lonko.gd` (3/3 pasando: contorno toon en escudo enemigo, separación de Lonko sin aglomeración final, y desviación de spawn por límite de Lonkos activos).
- FIX flechas flotando junto al pilar Lonko (clavadas en el aire a ~0.7 m): el X ancho (0.5→1.5 m en mundo) detenía la flecha en la cara de la caja, lejos del visual. Fix: X 0.5→0.28 (0.84 m en mundo ≈ visual 0.76 m) para que se claven en la superficie; Z se queda en 0.7 (2.1 m, invisible de lado y necesario para el contacto). REGLA actualizada: X ceñido al visual, Z nunca bajo 0.6 (test exige X≈0.84±0.15 y Z≥2.0 en mundo).
- FIX regresión pilar Lonko atravesado por flechas tras ajuste visual de colisión: el BoxShape quedó en Z 0.2 (0.61 m a escala 3), pero las flechas vuelan fijas en el plano Z de la jugadora (-7.5) y el pilar está en -6.75 (offset 0.45 a cámara): sin solape en Z el Area jamás detecta. Fix: Z 0.2→0.7 (2.1 m a escala 3, cubre el desfase 0.75 + margen); X se conserva en 0.5 (1.5 m, ciñe el visual). REGLA: no bajar Z de 0.6 en `PilarLonko.tscn` (el test `test_pilar_lonko_dano_flecha.gd` lo exige ≥2.0 m en mundo); el X sí puede ajustarse al gusto visual.
- Splash estilo Azulina (completo, un poco menor) al caer cañón y tuerca al agua: `CanonDestruidoVolador._generar_ondas_agua()` ya no recorta a ondas+burbujas (capas 1-2) → efecto completo de 6 capas como el emerger de Azulina (pilar, ondas, burbujas, gotas, impacto, remate); escalas 0.65→0.5 (cañón) y 0.38→0.3 (tuerca); la función ahora retorna el splash para testearlo. Tests en `test_canon_destruido_volador.gd` (2 nuevos: cañón 0.5 con todas las capas visibles, tuerca 0.3 menor que cañón y completa).
- Fin del Río: título centrado "Asalto al rio Superado" + botón Continuar + silencio total. `PantallaFinNivel`: el VBox con PRESET_CENTER no centraba (solo anclas) → nuevo `CentroVictoria` (CenterContainer full-rect) con VBox centrado, título centrado y botón SHRINK_CENTER debajo. Río pasa `clave_titulo_fin = RIO_ASALTO_SUPERADO` (fila nueva en translations.csv es+en; la pantalla mayuscula como el resto de títulos). Al cubrir (factor 1.0): `AudioManager.stop_all()` + barrido que detiene todo AudioStreamPlayer/3D en reproducción (loops fuera del grupo pausable: globo, canoa, tensados) + `get_tree().paused = true` para que nada re-arranque; Continuar reanuda antes de cambiar de escena (exports `detener_audio_al_cubrir`/`pausar_mundo_al_cubrir`). Tests en `test_transicion_fin_nivel_rio.gd` (3 nuevos: título del Río, centrado estructural, silencio→reanudar) + limpieza anti-leak (matar transiciones vivas y restaurar paused para no contaminar la suite).
- Línea negra 2D en globo y canasto que cae: `GloboAerostatico.asegurar_contorno_toon()` (patrón `AllyArcher`: grupo `outline_meshes` + next_pass TOON_LINEANEGRA negro ancho 20) sobre ModeloGlobo, ModeloGloboDestruido y ModeloCanasta, excluyendo tripulante (ciclo propio), VFX Fuego2D y sombras; se aplica en `_buscar_nodos_visuales()` y se re-asegura en `_eyectar_canasta()` tras el reset de `material_override` para que el canasto la conserve en la caída (única vía de creación de `CanastaCaida`, sin cambios ahí). Test nuevo `Tests/Unit/test_globo_outline.gd` (3: helper en malla suelta, escena real con contorno en los 3 modelos, canasta eyectada con daño letal conserva contorno bajo CanastaGloboCaida).
- Torre post-misión 5 → misión Río: Perrena NPC solo aparece tras la misión 5 (`PerrenaNPCInterior.aplicar_visibilidad_progresion()` lee `GameUI.regreso_conversacion_nivel5`; antes oculta, sin proceso/input/área); sin botón WIP regresar ni SALIR de mesa hasta escuchar el Plan de asalto (opción 0 del menú Perrena, `DIALOGO_ASALTO_*`); al terminar ese diálogo se marca `GameUI.plan_asalto_escuchado` y avisa a la torre (`torre_interior/desbloquear_mision_rio`), el botón pasa a `MENU_INICIAR_MISION` ("Iniciar misión"/"Start mission", fila nueva en translations.csv es+en) y al clic hay cortinilla circular a `Rio en canoa con paralax.tscn` limpiando `regreso_desde_interior_oleada` y `posicion_retorno_puerta`. `MesaConfiguracion.set_salir_visible()` + navegación ignora ocultos (SALIR es última: índices intactos). Lógica de puerta como estáticos testeables en `Interio` (`es_modo_post_mision5`, `mision_rio_desbloqueada`, `RUTA_NIVEL_RIO`, `TEXTO_INICIAR_MISION`). Test nuevo `Tests/Unit/test_torre_mision_rio.gd` (7). NOTA: reabrir el proyecto para reimportar translations.csv antes de exportar; post-Río la torre vuelve con "Iniciar misión" (rejugable): el paso a oleada 6 queda para tarea aparte.
- FIX pilar de Lonko indañable en el río (bug reportado: la flecha atravesaba el pilar sin registrar impacto): ruta de daño verificada intacta en fuente (máscara 71 de Arrow incluye capa 2 del PilarBody; `recibir_golpe`→flash+sonido→`_on_pilar_destruido` con 15 impactos; grupos enemies+escudos y flags es_escudo/es_pilar_enemigo correctos). Causa: hitbox de 0.84 m de ancho disparando a 8-12 m desde la canoa en movimiento + desfase de plano Z jugadora (-7.5) vs pilar (-6.75). Fix: `PilarLonko.tscn` BoxShape X 0.28→0.40 (1.2 m a escala 3, visual 0.76 m) y Z 0.85→1.20 (3.6 m, cubre el plano de la jugadora); Y intacta (3.0 m = visual). Test nuevo `Tests/Unit/test_pilar_lonko_dano_flecha.gd` (5: grupos/flags, capa vs máscara de flecha, cobertura Z del río, 15 golpes→aviso único sin duplicados, alias take_damage). Pendiente usuario: verificar en juego (flash rojo + sonido por impacto; 15 flechas normales o 2 explosivas destruyen el pilar y matan a Lonko).
- DISEÑO refuerzo reconstruye escudo destruido con 1 de vida + refuerzo a 1 (compensa que ya lo reparó al reaparecerlo): `AllyBallestera._regenerar_escudo_piso()` fija `golpes_para_destruir = 1` en la vía aliada (revierte el fix anterior que heredaba la vida original del nivel) y la rama de regeneración de `_aplicar_efecto_escudo_piso()` aplica `activar_modo_metalico(1)`; el refuerzo normal sobre escudo vivo sigue dando +2. Nuevo `Escudo.animar_reaparicion()` (dissolve + crecimiento vertical + colisiones off 1.2s, mismo lenguaje que `GameUI._animar_aparicion_escudo_disolucion`, con `_reaparicion_tween`, guards anti-flash y `_restaurar_material_estado()` al terminar para no pisar el metalizado) invocado desde `_regenerar_escudo_piso()` en vez de `_flash_dano()`. Test actualizado `test_bug_ballestera_refuerzo.gd::test_refuerzo_reconstruye_escudo_destruido_con_1_vida_y_1_refuerzo` (1 vida + aguante 1 + ID distinto + 1 aplicación).
- FIX escudo regenerado por refuerzo con 1 de vida (bug reportado): al destruirse el escudo de piso de una ballestera aliada, la habilidad "refuerzo" (primer disparo de fase agachada) lo regeneraba con `golpes_para_destruir = 1` hardcodeado (AllyBallestera `_regenerar_escudo_piso`) en vez de la vida real del nivel (4 en NIVEL01). Fix en 3 partes: (1) `AllyBallestera._vincular_escudo_piso()` captura `_escudo_piso_golpes` (nueva var) del escudo vinculado al enlazarlo; (2) `_regenerar_escudo_piso()` hereda esa vida en el escudo regenerado (fallback: copiarla de otro escudo aliado vivo vía `_buscar_escudo_aliado_referencia()`; `forzar_enemigo=true` conserva 1, API enemiga intacta); (3) el fallback sin escudos vivos cercanos ya NO pisa `_escudo_piso_transform` con identidad ni baja `_marco_escudo_es_real`: conserva el último marco real capturado → el escudo reaparece en su posición/escala exactas (0.6, no gigante 1.0). Test nuevo `test_bug_ballestera_refuerzo.gd::test_refuerzo_reaparece_escudo_destruido_con_vida_original` (aisla escudos a +1000, destruye el vinculado, aplica refuerzo, valida vida original + metálico + ID distinto).
- FIX ballesteras: sonido de refuerzo de escudo inaudible + gris metálico no perceptible. (1) `AudioManager.play_sfx("refuerzo_escudo")` ahora aplica +6 dB (el clip de 0.5s se perdía bajo el combate; mismo patrón que imp_death/muerte_critica). (2) `Escudo._crear_material_metalico()` usa la TEXTURA real `Escudo_metalico_refuerzo.jpg` (movida desde TEST_ a `Entities/Ambiente_Escudo/`) como `albedo_texture` sobre las piezas metálicas del modelo — visible en cualquier iluminación, sin depender de reflexiones ambientales; albedo_color escalado >1 para que el metal destaque; tests actualizados (`test_flecha_sobrecarga_guardiana_y_escudo_metalico.gd` 8/8). (3) `AllyBallestera._aplicar_efecto_escudo_piso()` re-vincula el escudo por cercanía si la referencia murió (GameUI reconstruye escudos entre oleadas → referencia huérfana) ANTES de caer a regenerar. → refuerzo 1 vez por fase + no acumulable máx +2: antes `AllyBallestera._disparar()` llamaba `_aplicar_efecto_escudo_piso()` en CADA uno de los 5 disparos agachados (5 SFX + 5 reactivaciones por ciclo); ahora solo en el primer tiro agachado (`fase_agachada and disparos_en_fase == 0`). Además no re-aplica si el escudo ya está al máximo (`es_metalico and aguante_metalico >= 2` → return sin sonido), con contador `refuerzos_aplicados`. Tests nuevos `test_bug_ballestera_refuerzo.gd` (5: vinculación real en NIVEL01, disparo agachado, 1 aplicación por fase en 5 tiros, no-acumulación al máximo, refresco hasta 2 si está gastado). FSM verificada end-to-end en NIVEL01 real: 5 disparos de pie (~25s) → fase agachada → SFX + metalizado (test_bug_ballestera_fsm.gd). NOTA diseño: ballesteras MÓVILES/mensajera se agachan visualmente pero NO refuerzan (no tienen escudo propio, `es_movil/es_mensajera` → return temprano) — decisión de diseño vigente. Fallo pre-existente ajeno: flecha normal hace 6 de daño a GuardianaMoradita (bono anti-estructuras aplicado a enemigo), test `test_flecha_normal_no_mata_guardiana_un_golpe` roto por otra causa.
- FIX power-up flecha explosiva/multiple congelado tras drop (no giraba, no sumaba, flotaba): causa raíz multiple. (1) `PowerUpFlechaExplosiva.gd`/`PowerUpFlechaMultiple.gd` nacian con escala 0.001 → Jolt Physics (engine activo del proyecto) rechaza el transform singular ("basis singular, scale of zero"), tratándolo como identidad y arriesgando estados internos inconsistentes del Area3D en el primer frame; ahora `ESCALA_SPAWN_MINIMA=0.05` constante + tween a ONE. (2) La disolución final tweenaba `scale` a `Vector3.ZERO` (otra vez singular); ahora escala mínima 0.05 + queue_free failsafe. (3) Watchdog de caida: si el tween de caida muere en pleno vuelo, `_process` detecta `_tween_caida` invalido y relanza `_comprobar_caida_al_suelo` (antes quedaba `_is_falling=true` eterno → flotando). (4) Respaldo `_tiempo_vivo` ya NO avanza con `get_tree().paused` (un item spawneado con el juego pausado no se auto-consumia de golpe al reanudar ni contaba tiempo de pausa). Tests: `test_bug_item_congelado.gd` (3: escala segura, watchdog de caida con tween killado, timer procesa en tiempo real), `test_power_up_flecha_explosiva.gd` 13/13, `test_power_up_flecha_multiple.gd` 5/5. Warning residual pre-existente ajeno: `PiernasHitbox` del Goblin (GoblinPiezaFisica hereda transform con escala del modelo al extraerse en muerte explosiva) — pendiente tarea separada.
- FIX raíz 27k warnings `ARQUERA_ALIADA.glb: Recurve Bow 2 Armature|ARCO_TENSAR Bone.003.L`: causa `_aplicar_animaciones_protagonista` copiaba librería `Recurve Bow 2 Armature` (26 tracks `Bone.003.L/R`, `IKRotateTarget`, `String`) del arco `GEO_ARCO_ANIMADO.fbx` (skeleton 51 huesos) al `AnimationPlayer` del personaje `ARQUERA_ALIADA.glb` (skeleton 78 `mixamorig:*`, sin `Bone.003`). Cada frame `AnimationMixer` intentaba resolver `Recurve Bow 2 Armature/Skeleton3D:Bone.003.L` en `Armature` → 27k errores. Fix: `AllyArcher.gd:222` y `AllyBallestera.gd:1147` filtran `Recurve Bow`/`ARCO` en `lib_name` y `anim_name`, seleccionan `AnimationPlayer` del personaje por `IDLE/DISPARO` no por `find_child` ciego, y purgan librerías contaminadas. `GEO_ARCO` sigue válido para `bow_anim_player` (`ARCO_TENSAR` 26 tracks OK en su esqueleto). Verificado `ARQUERA_ALIADA` 9 anims 119 tracks OK.
- TEST_ vaciado (320 archivos → 0): reubicados a arquitectura actual sin romper refs. Modelos: `Aliada_Ballestera/BALLESTERA_ALIADA.glb` → `Entities/Aliada_Ballestera/`, `Canasta/Canasta.glb`+LOD → `Entities/Enemigo_GloboAerostatico/`, `Canoa aliada/*` → `Entities/Ambiente_Canoa_Aliada/Canoa_aliada.*`, `Casco morrion/*` → `Entities/Ambiente_Casco_Morrion/`, `Goblo/Globo_*.glb` → `Entities/Enemigo_GloboAerostatico/`, `IMP_EXPLOTADO/*` → `Entities/Enemigo_Imp/` + `Tests/Unit/Test_Imp_Ragdoll.*`, `Flecha_Explosiva.*` → `Entities/Flecha_Explosiva/`, `Flecha_Multiple.*` → `Entities/Item_Flecha_Multiple/`, `Medikit/*` → `Entities/Item_Medikit/`, `ballesta repeticion.*` → `Entities/Aliada_Ballestera/Arma/`. Texturas: `arboleda.png` → `Ambiente_Arboleda/`, `enrredadera.png` → `Ambiente_Enrredadera/`, `pez.png` → `Ambiente_Pez/`, `pajaro.png` duplicado eliminado, `pergamino_*` → `Components/Dialogue/Textures/`, `test_*banner.png` → `Components/Dialogue/Test/`, `HUMO_PISADAS`+`Smoke*` → `VFX/Textures/Smoke/`, `Efecto linea manga.avif` → `VFX/Textures/`, `Icono_*` → `UI/Icons/` / `Entities/Ambiente_Escudo/` / `Entities/Item_Mensajera/`, `Mira mouse.png` → `UI/Icons/`. Audio: `Aura.mp3`+`Obtencion_arma`+`Parry`+`Refuerzo_escudo`+`Sonido_refuerzo_mensajera` → `System/Audio/SFX/`, `Fuego1`+`Impacto_pesado`+`Sonido_globo_callendo` → `Entities/Enemigo_GloboAerostatico/Audio/`, `Muerte/Risa_ballestera` → `Entities/Aliada_Ballestera/Audio/`. BinbunVFX: 235 duplicados borrados, 6 únicos movidos a `Assets/BinbunVFX/`. 30 archivos (.tscn/.tres/.gd) actualizados, 0 `res://TEST_/` restante.
- Globo Aerostatico explosion en cadena al morir: 3 explosiones rapidas en sucesion (0.7 s, copa->canasto siguiendo la caida) con los mismos recursos del pilar de Lonko destruido (Explocion_Pilar.tscn escala 0.75, rocas negras PIEDRAS_NEGRAS_DESTRUCION 12 part. y SFX EXPLOSION01/02 a -2.5 dB). Cadena con timers encadenados `_explotar_en_cadena -> _explotar_paso -> _spawn_explosion` (sin corrutinas: sin reanudacion sobre objeto liberado); fallback `get_tree().root` si current_scene es null (mismo patron que _drop_posion). Exports: cantidad_explosiones/intervalo_explosiones/escala_explosion. Tests: config, cadena 3 en sucesion con espia (boundary sin 4a) y VFX+rocas reales; globo 11/11.

## Completado
- **Imp — quitar RETROCEDER post-LANZAR01 + fix deformación GLB**: `ImpEnemy.gd:38` eliminados `@export tiempo_retroceder/offset_post_lanzar/desplazamiento_retroceder` y `is_retreating/retreat_timer:50`, `_process_shooting:424` ahora tras `LANZAR01/02` va directo a `IDLE` (`is_idle_pause`) sin `global_position.x += offset` ni `_play RETROCEDER`; `ImpEnemy.tscn:12` borrados `tiempo_retroceder/offset_post_lanzar` y bloque `Skeleton3D/bones` (80 líneas rancias con `2753.2854` etc) que desfasaban nuevo `IMP_ENEMIGO.glb` (hash `2b9bda2` vs `54fc99c`), `ImpModel` queda sin overrides (transform `0.005` conservado para escala horneada 100x); testear `Reimport` y ajustar escala a `1.0` si nuevo GLB ya viene a metros.
- **GameUI — preview fondo solo en editor + icono puerta uniforme**: `fondo_preview.png` (uid://bfv5gfrqj73t8) añadido a `GameUI.tscn:14` como `PreviewFondo` (Full Rect, mouse_filter IGNORE) detrás de `IconoPuerta`, script `UI/preview_fondo.gd@tool:6` hace `visible = Engine.is_editor_hint()` (visible en editor, oculto en runtime, textura sigue empaquetada); `IconoPuerta:26` reset `scale=1,1`, `stretch_mode=5 KEEP_ASPECT_CENTERED` + `expand_mode=1` garantiza textura sin deformar al redimensionar rect, script `UI/icono_puerta_uniforme.gd@tool:10` fuerza `scale.y = scale.x` en `_process` solo en editor para bloquear escalados tipo (1.0, -0.36). Posición actual 93,608->203,718 (110x110) pivot 55,55.
- **GoblinGirl — flecha en mano desfasada en juego**: usuario movió el modelo dentro de `GoblinGirlArrow.tscn` (compartida Lonko/GoblinGirl) y compensó con el transform de la instancia `FlechaMano` en `GoblinGirl.tscn:224`, pero al disparar `_actualizar_flecha_mano_durante_animacion()` pisaba `position = offset_flecha_mano` (0,0,0 por defecto) cada frame → desfase solo visible en runtime. Fix: nueva var `_pose_base_flecha_mano` capturada en `_configurar_flecha_visual_mano()` (rama nodo-de-escena y rama fallback), y los 3 resets de posición ahora usan `_pose_base_flecha_mano.origin` (+ shake). Tests GUT: captura de pose + regresión del pisado.
- **Lonko — anti-parpadeo + apuntado al cielo**: (1) Micro salto entre clips = `play(-1,...)` con `playback_default_blend_time=0` → seteado a `BLEND_ANIMACIONES=0.2` en ready; el snap del espejo de escala reemplazado por tween SINE de `scale:x` cruzando por 0 (`DURACION_ESPEJO_SG=0.15`, idempotente con kill de tween previo). (2) Ataque eléctrico sin apuntado: la carga activa `_is_invulnerable=true` y el gate viejo de `_process` bloqueaba el tracking si había invulnerabilidad → rama `_apuntar_arriba` (-90°) jamás corría. Fix: condición extraída a `_debe_rastrear_jugador()` donde `_apuntar_arriba` gana a la invulnerabilidad (daño sigue ganando a todo). Tests GUT nuevos/actualizados: `test_lonko_debe_rastrear_jugador_gate` (regresión especial, daño boundary, debug override, sin tracking) + espejo test adaptado a awaits por el tween.
- **Lonko — orientación IDLE + giro suave**: clip IDLE con yaw interno distinto; corrección por flags (`_obtener_yaw_objetivo_grados()`: pilar > idle > base) + snapeos reemplazados por `_aplicar_yaw_suave` (lerp_angle). Combo final validado por usuario: espejo ON + yaw 0 = perfil frontal mirando a la izquierda.
- Tareas antiguas resumidas: ragdoll v7 clalear torsión eje longitudinal ±0.08 rad; v6 resortes angulares stiffness=80; v5 límites endurecidos+damping; v4.1 fix cápsulas fuera del cuerpo (gen_tscn_v4.py); v4 joints 6DOF con límites + node_b dos niveles; GLB imp horneado a escala real 0.55 m (Python, IBM recomputadas); reorganización raíz proyecto (+340 rutas); fix artefactos AMD fullscreen; fix Parser Errors (Lonko.tscn packed arrays, Flecha_Electrica dano); polvo pisadas Lonko; VFX flecha eléctrica manual sincronizado; marca caída y=0.2 flecha traspasa estáticos; ataque eléctrico 2x rápido; orientación flecha eléctrica; recarga 2s; fix sobreescritura materiales VFX Gargola; VFX FireBall type B; addon UID materiales .tres embebido; Player Interior 3D WASD; walkable collider Esctrucutra2.

## Pendiente
- Usuario: verificar en juego el globo (avance lento, pausa de 10 s a mitad de la isla enemiga, disparo en movimiento con animacion GIRL_GOB_DISPARO y flecha magenta de GoblinGirl).
- Usuario: verificar en juego que la flecha de GoblinGirl sale ahora donde se afinó en el editor (y que Lonko no cambió).
- Verificar visualmente ragdoll en Test_Imp_Ragdoll.tscn (F6): cuerpo 0.55 m, cae y colisiona con piso.
- Reparar errores pre-existentes en suite completa (GargolaProjectile BOOST_VOLUMEN_X3_DB, Posion, PowerUps, Lonko, UIGameOver) — ajenos a esta tarea.

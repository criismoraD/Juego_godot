## Estado
- FIX raíz 27k warnings `ARQUERA_ALIADA.glb: Recurve Bow 2 Armature|ARCO_TENSAR Bone.003.L`: causa `_aplicar_animaciones_protagonista` copiaba librería `Recurve Bow 2 Armature` (26 tracks `Bone.003.L/R`, `IKRotateTarget`, `String`) del arco `GEO_ARCO_ANIMADO.fbx` (skeleton 51 huesos) al `AnimationPlayer` del personaje `ARQUERA_ALIADA.glb` (skeleton 78 `mixamorig:*`, sin `Bone.003`). Cada frame `AnimationMixer` intentaba resolver `Recurve Bow 2 Armature/Skeleton3D:Bone.003.L` en `Armature` → 27k errores. Fix: `AllyArcher.gd:222` y `AllyBallestera.gd:1147` filtran `Recurve Bow`/`ARCO` en `lib_name` y `anim_name`, seleccionan `AnimationPlayer` del personaje por `IDLE/DISPARO` no por `find_child` ciego, y purgan librerías contaminadas. `GEO_ARCO` sigue válido para `bow_anim_player` (`ARCO_TENSAR` 26 tracks OK en su esqueleto). Verificado `ARQUERA_ALIADA` 9 anims 119 tracks OK.
- TEST_ vaciado (320 archivos → 0): reubicados a arquitectura actual sin romper refs. Modelos: `Aliada_Ballestera/BALLESTERA_ALIADA.glb` → `Entities/Aliada_Ballestera/`, `Canasta/Canasta.glb`+LOD → `Entities/Enemigo_GloboAerostatico/`, `Canoa aliada/*` → `Entities/Ambiente_Canoa_Aliada/Canoa_aliada.*`, `Casco morrion/*` → `Entities/Ambiente_Casco_Morrion/`, `Goblo/Globo_*.glb` → `Entities/Enemigo_GloboAerostatico/`, `IMP_EXPLOTADO/*` → `Entities/Enemigo_Imp/` + `Tests/Unit/Test_Imp_Ragdoll.*`, `Flecha_Explosiva.*` → `Entities/Flecha_Explosiva/`, `Flecha_Multiple.*` → `Entities/Item_Flecha_Multiple/`, `Medikit/*` → `Entities/Item_Medikit/`, `ballesta repeticion.*` → `Entities/Aliada_Ballestera/Arma/`. Texturas: `arboleda.png` → `Ambiente_Arboleda/`, `enrredadera.png` → `Ambiente_Enrredadera/`, `pez.png` → `Ambiente_Pez/`, `pajaro.png` duplicado eliminado, `pergamino_*` → `Components/Dialogue/Textures/`, `test_*banner.png` → `Components/Dialogue/Test/`, `HUMO_PISADAS`+`Smoke*` → `VFX/Textures/Smoke/`, `Efecto linea manga.avif` → `VFX/Textures/`, `Icono_*` → `UI/Icons/` / `Entities/Ambiente_Escudo/` / `Entities/Item_Mensajera/`, `Mira mouse.png` → `UI/Icons/`. Audio: `Aura.mp3`+`Obtencion_arma`+`Parry`+`Refuerzo_escudo`+`Sonido_refuerzo_mensajera` → `System/Audio/SFX/`, `Fuego1`+`Impacto_pesado`+`Sonido_globo_callendo` → `Entities/Enemigo_GloboAerostatico/Audio/`, `Muerte/Risa_ballestera` → `Entities/Aliada_Ballestera/Audio/`. BinbunVFX: 235 duplicados borrados, 6 únicos movidos a `Assets/BinbunVFX/`. 30 archivos (.tscn/.tres/.gd) actualizados, 0 `res://TEST_/` restante.
- Globo Aerostatico explosion en cadena al morir: 3 explosiones rapidas en sucesion (0.7 s, copa->canasto siguiendo la caida) con los mismos recursos del pilar de Lonko destruido (Explocion_Pilar.tscn escala 0.75, rocas negras PIEDRAS_NEGRAS_DESTRUCION 12 part. y SFX EXPLOSION01/02 a -2.5 dB). Cadena con timers encadenados `_explotar_en_cadena -> _explotar_paso -> _spawn_explosion` (sin corrutinas: sin reanudacion sobre objeto liberado); fallback `get_tree().root` si current_scene es null (mismo patron que _drop_posion). Exports: cantidad_explosiones/intervalo_explosiones/escala_explosion. Tests: config, cadena 3 en sucesion con espia (boundary sin 4a) y VFX+rocas reales; globo 11/11.

## Completado
- **Imp — quitar RETROCEDER post-LANZAR01 + fix deformación GLB**: `ImpEnemy.gd:38` eliminados `@export tiempo_retroceder/offset_post_lanzar/desplazamiento_retroceder` y `is_retreating/retreat_timer:50`, `_process_shooting:424` ahora tras `LANZAR01/02` va directo a `IDLE` (`is_idle_pause`) sin `global_position.x += offset` ni `_play RETROCEDER`; `ImpEnemy.tscn:12` borrados `tiempo_retroceder/offset_post_lanzar` y bloque `Skeleton3D/bones` (80 líneas rancias con `2753.2854` etc) que desfasaban nuevo `IMP_ENEMIGO.glb` (hash `2b9bda2` vs `54fc99c`), `ImpModel` queda sin overrides (transform `0.005` conservado para escala horneada 100x); testear `Reimport` y ajustar escala a `1.0` si nuevo GLB ya viene a metros.
- **GameUI — preview fondo solo en editor + icono puerta uniforme**: `fondo_preview.png` (uid://bfv5gfrqj73t8) añadido a `GameUI.tscn:14` como `PreviewFondo` (Full Rect, mouse_filter IGNORE) detrás de `IconoPuerta`, script `UI/preview_fondo.gd@tool:6` hace `visible = Engine.is_editor_hint()` (visible en editor, oculto en runtime, textura sigue empaquetada); `IconoPuerta:26` reset `scale=1,1`, `stretch_mode=5 KEEP_ASPECT_CENTERED` + `expand_mode=1` garantiza textura sin deformar al redimensionar rect, script `UI/icono_puerta_uniforme.gd@tool:10` fuerza `scale.y = scale.x` en `_process` solo en editor para bloquear escalados tipo (1.0, -0.36). Posición actual 93,608->203,718 (110x110) pivot 55,55.
- **GoblinGirl — flecha en mano desfasada en juego**: usuario movió el modelo dentro de `GoblinGirlArrow.tscn` (compartida Lonko/GoblinGirl) y compensó con el transform de la instancia `FlechaMano` en `GoblinGirl.tscn:224`, pero al disparar `_actualizar_flecha_mano_durante_animacion()` pisaba `position = offset_flecha_mano` (0,0,0 por defecto) cada frame → desfase solo visible en runtime. Fix: nueva var `_pose_base_flecha_mano` capturada en `_configurar_flecha_visual_mano()` (rama nodo-de-escena y rama fallback), y los 3 resets de posición ahora usan `_pose_base_flecha_mano.origin` (+ shake). Tests GUT: captura de pose + regresión del pisado.
- **Lonko — anti-parpadeo + apuntado al cielo**: (1) Micro salto entre clips = `play(-1,...)` con `playback_default_blend_time=0` → seteado a `BLEND_ANIMACIONES=0.2` en ready; el snap del espejo de escala reemplazado por tween SINE de `scale:x` cruzando por 0 (`DURACION_ESPEJO_SG=0.15`, idempotente con kill de tween previo). (2) Ataque eléctrico sin apuntado: la carga activa `_is_invulnerable=true` y el gate viejo de `_process` bloqueaba el tracking si había invulnerabilidad → rama `_apuntar_arriba` (-90°) jamás corría. Fix: condición extraída a `_debe_rastrear_jugador()` donde `_apuntar_arriba` gana a la invulnerabilidad (daño sigue ganando a todo). Tests GUT nuevos/actualizados: `test_lonko_debe_rastrear_jugador_gate` (regresión especial, daño boundary, debug override, sin tracking) + espejo test adaptado a awaits por el tween.
- **Lonko — orientación IDLE + giro suave**: clip IDLE con yaw interno distinto; corrección por flags (`_obtener_yaw_objetivo_grados()`: pilar > idle > base) + snapeos reemplazados por `_aplicar_yaw_suave` (lerp_angle). Combo final validado por usuario: espejo ON + yaw 0 = perfil frontal mirando a la izquierda.
- Tareas antiguas resumidas: ragdoll v7 clalear torsión eje longitudinal ±0.08 rad; v6 resortes angulares stiffness=80; v5 límites endurecidos+damping; v4.1 fix cápsulas fuera del cuerpo (gen_tscn_v4.py); v4 joints 6DOF con límites + node_b dos niveles; GLB imp horneado a escala real 0.55 m (Python, IBM recomputadas); reorganización raíz proyecto (+340 rutas); fix artefactos AMD fullscreen; fix Parser Errors (Lonko.tscn packed arrays, Flecha_Electrica dano); polvo pisadas Lonko; VFX flecha eléctrica manual sincronizado; marca caída y=0.2 flecha traspasa estáticos; ataque eléctrico 2x rápido; orientación flecha eléctrica; recarga 2s; fix sobreescritura materiales VFX Gargola; VFX FireBall type B; addon UID materiales .tres embebido; Player Interior 3D WASD; walkable collider Esctrucutra2.

## Pendiente
- Usuario: verificar en juego el globo (avance lento, pausa de 10 s a mitad de la isla enemiga, disparo en movimiento con animacion GIRL_GOB_DISPARO y flecha magenta de GoblinGirl).
- Usuario: verificar en juego que la flecha de GoblinGirl sale ahora donde se afinó en el editor (y que Lonko no cambió).
- Verificar visualmente ragdoll en Test_Imp_Ragdoll.tscn (F6): cuerpo 0.55 m, cae y colisiona con piso.
- Reparar errores pre-existentes en suite completa (GargolaProjectile BOOST_VOLUMEN_X3_DB, Posion, PowerUps, Lonko, UIGameOver) — ajenos a esta tarea.

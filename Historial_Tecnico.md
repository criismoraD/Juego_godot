## Estado
- Punto estable: Estructura del proyecto reorganizada y optimizada.
- 59 de 59 pruebas unitarias pasando exitosamente en GUT (100% de éxito).
- Escudos, enemigos, proyectiles, interfaz y utilidades completamente ordenados y funcionales con rutas corregidas.

## Completado
- Corrección de Pruebas Unitarias de Sombra: Solucionado error en [test_sombra_personaje.gd](file:///e:/Users/criis/Descargas/ARQUERA_JUEGO/Tests/Unit/test_sombra_personaje.gd) cambiando el orden de instanciación para que el nodo padre esté en el árbol antes de configurar su posición global. Ajustada la limpieza en `after_each` liberando las instancias de sombra creadas en cada test para eliminar pérdidas de memoria (3 huérfanos resueltos). Logrado el 100% de éxito en GUT (59/59 pasados).
- Sistema de Partículas de Hojas Caídas en NIVEL01: Creado y configurado el nodo `Hojas_Particulas` (de tipo `GPUParticles2D`) en el `Compositor3D` de [NIVEL01.tscn](file:///e:/Users/criis/Descargas/ARQUERA_JUEGO/Levels/NIVEL01/NIVEL01.tscn) usando la textura `hojas.png`. Escaladas las dimensiones a pantalla completa (Emission Box a `1200x600`, Visibility Rect a `14000x1200`), optimizada la gravedad a `120`, incrementada la velocidad lineal (`100` a `250`), aumentado amount a `200`, configurada duración de vida (`lifetime = 8.0`) y reducidas las dimensiones de las hojas 10 veces (`scale` de `0.05` a `0.12`).
- Ajuste de Velocidad de Flechas por Ángulo: Implementada la reducción dinámica de velocidad en proyectiles del jugador mediante un factor basado en la inclinación vertical (`Factor_Angulo`) para prevenir que las flechas salgan demasiado rápido de la pantalla al disparar en ángulo.
- Tareas Antiguas: Ajuste de animación de muerte de ImpShieldGirl con shader de disolución; reorganización arquitectónica modular del proyecto; corrección de Rutas y UIDs de Godot 4; resolución de errores de shader y reconstrucción de escudos en Debug UI (Oleada 3 y Nivel 3 estables); comportamiento dinámico de `ImpShieldGirl` protegiendo enemigos vivos; sistema de sombras procedurales optimizado; soporte para shader toon en proyectiles, colisiones, alineaciones de fondos y luciérnagas.

- Ragdoll Gárgola al morir: Reemplazada textura piedra (`PIEDRA_D.jpg`) por ragdoll físico en [Gargola.gd](file:///e:/Users/criis/Descargas/ARQUERA_JUEGO/Entities/Enemies/GARGOLA/Gargola.gd). Removidos `_aplicar_textura_piedra`, `textura_piedra`, `cayendo`, `altura_suelo_muerte` y exports muertos (`velocidad_caida`, `tiempo_disolucion_muerte`). Añadidas categorías `@export_category("Ragdoll - Gargola")` con `huesos_ragdoll` (24 huesos deformantes Rigify + alas), `masa_hueso_ragdoll` (0.5), `radio_colision_ragdoll` (0.08), `tiempo_espera_disolucion_trapo` (2.5). `_construir_ragdoll()` se ejecuta SOLO al morir (construcción perezosa en `_activar_ragdoll`). Crea `PhysicalBoneSimulator3D` como padre de los `PhysicalBone3D` (requisito API Godot 4). Los huesos objetivo se deducen en runtime desde el `Skin` del mesh (`_obtener_huesos_deformantes`); además se incluyen TODOS los huesos ancestros hasta la raíz para que la jerarquía sea continua y ninguna parte (cabeza, alas) quede suelta/volando. Huesos raíz = `JOINT_TYPE_NONE` (caen libres); el resto `JOINT_TYPE_CONE` (ball joint) con `linear_damp`/`angular_damp` (`amortiguacion_ragdoll` 0.5) para estabilizar el impacto. NOTA Godot 4.7: `PhysicalBone3D` NO expone `joint_swing`/`joint_twist`/`simulate_physics` (el cone no lleva límites configurables y la simulación se controla solo con `physical_bones_start_simulation`). FIX ESTRUCTURAL (raíz del "estiramiento"): el código NO asignaba `body_offset` ni `joint_offset` (quedaban en identidad), así que los cuerpos físicos y las articulaciones quedaban mal posicionados respecto a los huesos y el solver estiraba la malla por toda la pantalla. Se replicó fielmente el patrón del editor de Godot (`editor/scene/3d/skeleton_3d_editor_plugin.cpp::create_physical_bone`): por cada hueso se calcula la dirección a su hijo (`skeleton.get_bone_rest(child)`), se crea una **cápsula** dimensionada al largo del hueso (`height = largo`, `radius = largo*0.2` vía `grosor_capsula_ragdoll`), la `shape.transform` rota la cápsula (eje Y→Z), `body_offset = Basis.looking_at(dir_hijo, up)` con origen a medio hueso, y `joint_offset = Transform3D(IDENTITY, Vector3(0,0,medio_largo))`. `joint_type = NONE` para raíz/hijos directos y `CONE` para el resto (queda "trapo"). Helper `_obtener_hijo_objetivo(idx, indices)`. La simulación sigue arrancando 1 frame después (`ragdoll_inicio_pendiente` → `_iniciar_simulacion_ragdoll`) y el AnimationPlayer queda desactivado. `gravedad_ragdoll` 0.4, `impulso_horizontal_ragdoll` 0.8, `masa_hueso_ragdoll` 1.5, amortiguación REPLACE, `can_sleep`. Colisiones layer 4 / mask 1. Test en `Tests/Unit/test_gargola_ragdoll.gd`.

## Pendiente
- Asegurar soporte de traducción completo.

- Fix Sobreescritura de Materiales VFX FireBall (`GargolaProjectile.gd`): causa raíz = `EnemyProjectileBase._ready` cachea TODOS los `MeshInstance3D` hijos (recursivo, `owned=false`) incluyendo los internos del VFX, y el método base `_restaurar_visuales_desde_pool` aplica `material_override = projectile_material` (rojo toon + outline negro) sobre ellos. Solución: override de `_ready` en `GargolaProjectile` que tras `super._ready()` filtra (`_excluir_meshes_vfx_del_cache`) los meshes del VFX fuera de `_cached_mesh_instances`. Defensa en profundidad junto al override existente de `_restaurar_visuales_desde_pool`/`_cleanup_materials`. Test `test_gargola_projectile.gd` reescrito a comportamiento VFX.
- Cambio de VFX a `VFX_Fire_ball_type_B.tscn`: reemplazada la constante `VFX_FIREBALL_SCENE` (antes `VFX_Fire_ball_standar.tscn`). Ajustada la compensación de escala a `0.25 / scale.x` (escala total del VFX en mundo = 0.25) y rotación del VFX a `rotation.y = -PI/2` para alinear su eje de avance (sobre Z) con el +X local del proyectil, de modo que apunte a la izquierda al disparar. Test actualizado al root `VFX_FireBall_TypeB`. Requiere recompilar/clear `.godot`.

### Reciente
- Player Interior 3D: Creado `Entities/Player/Player_Interior.gd` + `.tscn`. Nuevo personaje sin disparo con movimiento 3D WASD en plano XZ. Rotación del modelo `ArqueraModel` hacia 4 cardinales (eje dominante: N → -Z/W, S → +Z/S, E → +X/D, O → -X/A) usando `atan2(-d.z, d.x)` + `lerp_angle` suave (10°/s). AnimationTree dinámico con solo Idle ↔ Walk (`CAMINAR_ADELANTE`) vía `AnimationNodeTransition` xfade 0.2s. Mismo modelo PROTA.glb + CapsuleShape3D (ø0.4, h1.98) que Player original. Sin arco animado, sin huesos overrides, script mínimo ~105 líneas. Reemplazada instancia de `Player.tscn` en `Interio.tscn` por `Player_Interior.tscn` (misma posición/escala 0.09).
- Walkable Collider `Esctrucutra2` en `Nivel_Interior/Interio.tscn`: Convertido nodo a `StaticBody3D` + 4 `CollisionShape3D`, cada uno con BoxShape3D independiente para edición libre en editor.
- Addon Godot `addons/arquera_godot_tools/plugin.gd`: refactor `_asignar_uid_material`. Causa raíz: `.tres` files de Godot 4.7 embeben UID en **header del propio `.tres`** (línea `uid="uid://..."`), NO usan archivo `.uid` separado (como sí hacen `.gd`). Fix anterior escribía `.uid` file erróneamente. Reescrito para: (1) `get_resource_uid` (cache de ResourceLoader), (2) fallback parseo header `.tres` con `get_slice("\n",0)` + `find('uid="')`, (3) vacío como último fallback. `fs.scan()` movido ANTES de `_asignar_uid_material` para que Godot registre el `.tres` recién guardado y le embeba UID en el header. Añadidos `print("[ARQUERA] ...")` diagnósticos en 4 puntos clave para debug.
- Fix Material Externo Addon Godot `addons/arquera_godot_tools/plugin.gd`: causa raíz = `.uid` file NUNCA se escribía a disco. Función ahora (1) lee UID existente de `ResourceLoader`, (2) lee `.uid` file si existe, (3) crea UID + escribe `.uid` file manualmente con `FileAccess` (formato `uid://<id>\n`).
- Addon Godot `addons/arquera_godot_tools/plugin.gd`: reemplazados 2 botones por 1 "Procesar" + 2 CheckBox (`Check_Next_Pass`, `Check_Unshaded`). `_crear_material_final` ahora aplica `SHADING_MODE_UNSHADED` y `next_pass` solo si checkbox activo.
